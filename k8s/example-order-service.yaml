apiVersion: v1
kind: Service
metadata:
  name: example-order
  namespace: example-order-namespace
spec:
  selector:
    app: example-order
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080